# Basic Expense Tracker using HTML, CSS and JS to keep a track of daily/monthly expense 
the tracker uses local storage of web browser to store data and gives notification for adding or deleting entries.
